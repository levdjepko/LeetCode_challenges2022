class Solution {
    public boolean isValidSudoku(char[][] board) {
        // check if Sudoku board is valid
        // check rows:
        for (int i = 0; i < board.length; i++) {
            
        }
    }
}
